## Online Movie Ticket Booking Platform

A backend service that enables browsing movies and shows, seat selection, and booking tickets with transactional consistency and scalability in mind.

API contracts are documented using OpenAPI 3.0 and are available at `docs/openapi.yaml` for integration and reference.
